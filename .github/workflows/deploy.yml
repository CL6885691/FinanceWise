name: Deploy FinanceWise to GitHub Pages

on:
  push:
    branches:
      - main # 如果您的預設分支是 master，請在此改為 master

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build
        env:
          # 重要：這裡的 API_KEY 必須對應您在 GitHub Secrets 設定的名稱
          API_KEY: name: Deploy FinanceWise to GitHub Pages

on:
  push:
    branches:
      - main # 如果您的預設分支是 master，請在此改為 master

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Set up Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build
        env:
          # 重要：這裡的 API_KEY 必須對應您在 GitHub Secrets 設定的名稱
          API_KEY: AIzaSyDfGI_4MM6QPiFtPA_lvOnxB2AKZQWYSEk
          # 如果您有在 Secrets 設定 FIREBASE_CONFIG，也會在此注入
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 預設的輸出目錄
          branch: gh-pages # 部署到的分支

          # 如果您有在 Secrets 設定 FIREBASE_CONFIG，也會在此注入
          FIREBASE_CONFIG: ${{ secrets.FIREBASE_CONFIG }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite 預設的輸出目錄
          branch: gh-pages # 部署到的分支
